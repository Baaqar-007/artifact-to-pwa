{
  "name": "artifact-to-pwa",
  "version": "2.0.0",
  "description": "Convert any Claude artifact (HTML/React/JSX) or public URL into a single self-contained HTML file — open by double-click, no build step",
  "type": "module",
  "bin": {
    "artifact-to-pwa": "./bin/cli.js"
  },
  "files": [
    "bin/",
    "src/",
    "README.md"
  ],
  "keywords": [
    "pwa", "claude", "artifact", "cli", "html",
    "react", "jsx", "offline", "standalone", "converter", "localstorage"
  ],
  "author": "",
  "license": "MIT",
  "engines": { "node": ">=18" },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0"
  }
}
